#!/usr/bin/env bash

set -euo pipefail

if ! [ -x "$(command omz -v)" ]; then
	/usr/bin/ruby -e "$(sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)")"
fi
